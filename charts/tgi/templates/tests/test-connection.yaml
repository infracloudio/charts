apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tgi.fullname" . }}-test-connection"
  labels:
    {{- include "tgi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tgi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
